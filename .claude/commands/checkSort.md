# configSort.yaml 检查

请读取 Output/AquaMai.zh.toml 和 AquaMai/configSort.yaml 两个文件，检查以下内容：

- AquaMai.zh.toml 中的所有 Sections，是否都在 configSort.yaml 中存在（必须都存在）
- configSort.yaml 中，“社区功能”分类中的内容，是否同时存在于其他分类中（必须都存在）

configSort.yaml 中的内容可以不存在于 AquaMai.zh.toml 中

如果以上检查有任何不通过，请告知不通过检查的 Section 名称。否则，请告知检查通过

请不要在回复中列出读取到已通过检查的 Section 名称
